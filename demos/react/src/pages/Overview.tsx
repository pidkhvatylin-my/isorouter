export default function Overview() {
  return (
    <>
      <h2>Overview</h2>
      <p>
        Dashboard index route — rendered via <code>&lt;Outlet&gt;</code> inside
        DashboardLayout.
      </p>
    </>
  );
}
