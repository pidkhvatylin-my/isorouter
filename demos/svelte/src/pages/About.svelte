<h1>About</h1>

<p>
  <code>@isorouter/svelte</code> is a thin Svelte 5 adapter over
  <code>@isorouter/core</code>, a Navigation API–first isomorphic router.
</p>

<p>
  State is bridged to Svelte runes via <code>createSubscriber</code> — no stores,
  no effects, no leaks.
</p>
